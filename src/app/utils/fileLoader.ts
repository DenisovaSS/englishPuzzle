export function getAudioFileURL(fileName: string): string {
  return `https://denisovass.github.io/englishPuzzle-data/${fileName}`;
}
export function getImgURL(fileName: string): string {
  return `https://denisovass.github.io/englishPuzzle-data/img/${fileName}`;
}
